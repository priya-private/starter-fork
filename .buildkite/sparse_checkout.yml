steps:
  - label: Pipeline upload
    command: echo "runnign sparse"
    key: "step1"
    plugins:
      - sparse-checkout:
          paths:
            - .buildkite

  - label: "test"
    command: echo "testing heading"
    depends_on: step1

  
