---
steps:
- key: dd_key_test_one
  label: test
  command: echo "$BUILDKITE_STEP_KEY" | buildkite-agent meta-data set "dd_tags.team" $BUILDKITE_STEP_KEY
- key: two
  label: test1
  command: echo "$BUILDKITE_PIPELINE_SLUG" | buildkite-agent meta-data set "dd_tags.test_key" $BUILDKITE_PIPELINE_SLUG